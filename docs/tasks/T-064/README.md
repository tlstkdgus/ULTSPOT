# T-064 · 문서 정리 — 태스크 목록 표 하나로, README 첫 화면 캡처 교체

| | |
|---|---|
| 상태 | 완료 |
| 브랜치 | `docs/T-064-cleanup` (기준: `main` 5a1a4a8) |
| 기간 | 2026-09-25 |
| 근거 | 남은 정리 목록(2026-09-25) — 태스크 목록 머리글 위치, README 상단 캡처 |

## 바꾼 것

- `docs/tasks/README.md`: 표가 둘로 갈라져 있었다. 첫 표는 머리글이 "신규 태스크 | 내용"인데 행은 5열이었고, 설명 문장과 진짜
  머리글이 파일 중간에 있었다. **표 하나로 합치고 번호순으로 정렬**했다. 62행을 모두 옮겼고(중복 0), 2열뿐이던 T-030·T-031은
  빈 칸을 "—"로 채웠다.
- `README.md` 첫 이미지: T-005(9월 17일 비회원 진입 화면) → T-058 홈(지금 화면)과 T-061 일정 받기(지도·빈 시간 추천).

## 하지 않은 것

- 로컬 전체 실행에서만 가끔 멈추는 생일카페 사진 검사(fan-cafes mobile). 원인을 특정하지 못했다(T-048 기록). 프로덕션
  check:prod에서는 매번 통과한다. 재현될 때 트레이스를 붙이는 방식은 그대로 둔다.
- 행 내용(상태·PR 번호)의 과거 값 정정. 옛 행의 "리뷰 중"·"PR 준비"는 당시 기록이라 손대지 않았다.

## 검증

- 문서만 변경. 표 행 수 전후 62 → 62(+T-064), 이미지 경로 두 개가 저장소에 있는 것을 확인했다.
