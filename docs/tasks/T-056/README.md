# T-056 · README를 지금 기능·외부 API·환경 변수에 맞춘다

| | |
|---|---|
| 상태 | 완료 |
| 브랜치 | `docs/T-056-readme` (기준: `main` cdd4ed4) |
| 기간 | 2026-09-25 |
| 근거 | 사용자 요청(2026-09-25) "readme에 옮겨" |

## 바꾼 것

- **무엇을 하나** 절 신설: 최애 → SPOT → 기간 → 일정, 빈 시간 채우기(영업시간·사진), 여러 날·현장 기록. "순위만 Jev, 시간은 코드" 원칙.
- 기술 표: "AI · OpenAI (예정)"을 실제 쓰는 것으로 바꿨다 — Jev(순위), 카카오 로컬·길찾기, TourAPI, Google Maps, Vercel Analytics·GA4.
- 환경 변수 표: 4개 → 11개. 노출 범위, **Vercel 유형(Config/Secret)**, 비어 있을 때의 동작, 서버 일일 상한.
- 스크립트 표: `test:e2e`·`check:prod` 설명을 지금 동작(전체 E2E)에 맞추고 `test:data` 추가.
- 태스크 목록: T-049~T-055 상태를 완료와 실제 PR 번호로. T-050 기록에 #64가 잘못된 브랜치로 머지된 경위를 남겼다.

## 하지 않은 것

- Manyfast 반영. 이 세션에서 Manyfast 연결(MCP)이 인증되지 않아 쓸 수 없다. 사용자가 연결을 인증하면 옮긴다.
- README 상단의 T-005 캡처 교체. 지금 홈 화면과 다르지만 캡처 교체는 별도 작업이다.

## 검증

- 문서만 바뀐다. 표의 "없으면" 동작은 코드에서 확인했다(`kakao.ts`·`api/recommend`·`tour.ts`·`google.ts`·`layout.tsx`).
