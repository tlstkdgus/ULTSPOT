# 작업 규칙 — 브랜치 · 커밋 · PR · 태스크 기록 · 문서

ULTSPOT은 **보여지는 것이 곧 제품**인 서비스다. 그래서 이 규칙의 중심에는 두 가지가 있다.

1. **모든 작업은 기록이 남는다.** 태스크 문서, 커밋 본문, PR 본문이 각각 다른 질문에 답한다.
2. **화면을 바꾼 작업은 캡처가 남는다.** 3개 뷰포트 스크린샷 없이 머지하지 않는다.

---

## 1. 작업 흐름 한눈에

```
태스크 번호 받기 (T-00N)
  → 브랜치 생성  feat/T-00N-slug
  → 작업 단위마다 커밋 + push        ← 매 커밋 push. 로컬에만 쌓아두지 않는다
  → UI를 바꿨으면 pnpm capture T-00N → 스크린샷 커밋
  → docs/tasks/T-00N/README.md 기록
  → PR 생성 (템플릿 채우기)
  → 셀프 리뷰 (gh pr diff) → Merge commit (squash 금지)
  → main 머지 = Vercel 프로덕션 자동 배포 → pnpm check:prod <프로덕션 URL>
```

---

## 2. 브랜치 — GitHub Flow

`main` 하나를 기준으로 짧은 작업 브랜치를 판다. `develop` 같은 장수 브랜치는 두지 않는다.
혼자 또는 소수가 빠르게 화면을 쌓는 단계라, 브랜치 계층이 늘면 병합 비용만 커진다.

| 브랜치 | 용도 | 예시 |
|--------|------|------|
| `main` | 항상 빌드·배포 가능한 상태. **직접 push 금지** | |
| `feat/T-NNN-<slug>` | 기능·화면 추가 | `feat/T-002-trip-date-picker` |
| `fix/T-NNN-<slug>` | 버그 수정 | `fix/T-014-safari-font-flash` |
| `design/T-NNN-<slug>` | 토큰·컴포넌트·비주얼만 바꾸는 작업 | `design/T-005-badge-contrast` |
| `chore/` `docs/` `refactor/` `test/` + `T-NNN-<slug>` | 설정 · 문서 · 리팩터 · 테스트 | `chore/T-001-project-setup` |

- **태스크 번호를 브랜치에 넣는다.** 브랜치 → 태스크 문서 → 스크린샷 폴더가 같은 번호로 묶인다.
- slug는 kebab-case, 범위가 드러나게. `feat/T-003-screens`(뭉뚱그림) ✗ → `feat/T-003-event-card-list` ✓
- 브랜치가 3일 넘게 살아있거나 PR diff가 500줄(에셋·스크린샷·lockfile 제외)을 넘으면 쪼갤 신호다.
- `design/`을 따로 둔 이유: 이 서비스는 비주얼만 바꾸는 작업이 잦고, 기능 변경과 섞이면
  "화면이 달라진 게 의도인가 부작용인가"를 리뷰에서 구분할 수 없다.

---

## 3. 커밋

### 형식 — Conventional Commits, 제목은 한국어

```
<type>(<scope>): <무엇이 달라지는지 한 줄>

<본문 — 아래 "본문에 쓰는 것">

Co-Authored-By: ... (AI와 함께 작업했으면)
```

**type**

| type | 언제 |
|------|------|
| `feat` | 사용자가 보거나 쓸 수 있는 기능·화면 추가 |
| `fix` | 버그 수정 |
| `design` | 토큰·컴포넌트 스타일·레이아웃 등 비주얼 변경 (동작 변화 없음) |
| `refactor` | 동작·화면 변화 없는 구조 변경 |
| `perf` | 성능 |
| `test` | 테스트·캡처 파이프라인 |
| `docs` | 문서·태스크 기록 |
| `chore` | 설정·의존성·빌드 |

**scope** (선택, 붙이면 로그가 읽힌다) — `ui` `brand` `tokens` `home` `trip` `event` `map` `auth` `db` `ai` `e2e` `infra`

**제목** — 한국어, 50자 안팎, 마침표 없음. "수정", "정리"처럼 무엇인지 안 드러나는 단어만 쓰지 않는다.

```
feat(trip): 여행 날짜를 고르면 그 기간 이벤트만 목록에 남긴다
design(tokens): 마감임박 뱃지 글자색을 잉크로 바꿔 대비 4.5:1 확보
fix(event): 종료일 당일 이벤트가 마감으로 표시되던 문제
```

### 작업 단위마다 커밋하고 바로 push

- 커밋 하나 = 되돌릴 수 있는 의미 단위 하나. "하루치 작업" 커밋은 만들지 않는다.
- 커밋할 때마다 `git push` 한다. 로컬에만 있는 커밋은 기록이 아니다.
- 각 커밋은 **그 시점에 빌드가 되는 상태**여야 한다 (`pnpm lint && pnpm typecheck`).

### 본문에 쓰는 것 — diff에 없는 것만

diff는 *무엇이* 바뀌었는지 이미 보여준다. 본문은 그 바깥을 쓴다.

1. **왜 이 방식인가** — 버린 대안이 있으면 왜 버렸는지까지
2. **근거 참조** — 기획서·브랜드 가이드·결정에서 나온 동작은 출처를 단다 (`브랜드 가이드 v1.0 Don't`, `기획서 §3.2`)
3. **하지 않은 것** — 의도적으로 뺀 것과 이유. 다음 사람이 "빠뜨렸나?" 하고 다시 만드는 걸 막는다
4. **검증을 숫자로** — "테스트 통과" ✗ → "test:e2e 9건 통과, 캡처 6장" ✓. 안 돌렸으면 안 돌렸다고 쓴다. 실패가 있었으면 그대로 쓴다
5. **과거 사고 이력** — 같은 자리를 다시 건드릴 사람을 위해

```
# 나쁨 — diff 요약일 뿐
- Badge 컴포넌트 추가
- tone prop 추가

# 좋음 — 판단이 남는다
- 상태 뱃지(진행중)는 초록. 가이드 목업은 오렌지지만 같은 가이드 Don't
  "상태 뱃지에 브랜드 컬러 금지"와 충돌해 규칙 쪽을 따랐다
- 하지 않은 것: 아이콘 슬롯 — 기획서에 아이콘 뱃지가 아직 없다
```

본문 생략은 오탈자·의존성 버전 올림처럼 자명한 커밋만 허용한다.

---

## 4. 화면 캡처 (UI를 바꾼 작업은 필수)

```bash
pnpm exec playwright install chromium   # 최초 1회
pnpm capture T-002                      # e2e/routes.ts 의 모든 화면 × 3 뷰포트
pnpm capture T-002 home,design-system   # 일부 화면만
```

- 결과: `docs/tasks/T-002/screenshots/<화면>-<mobile|tablet|desktop>.png`
- 뷰포트: **mobile 390(@2x) / tablet 768 / desktop 1440**. 반응형이라 셋 다 찍는다.
- 프로덕션 빌드(`build` + `start`)로 찍고, 웹폰트 로딩을 기다린 뒤 찍는다. dev 서버 화면은 기록으로 쓰지 않는다.
- 새 화면을 만들면 `e2e/routes.ts`에 추가한다. 캡처와 "가로 스크롤 없음" 검사가 같이 따라온다.
- 로그인 뒤 화면·모달처럼 경로만으로 못 여는 상태는 `e2e/capture.spec.ts`에 단계를 추가하거나,
  브라우저에서 수동 캡처해 같은 폴더에 `<화면>-<상태>-<뷰포트>.png`로 넣는다.
- 기존 화면을 바꿨으면 **before/after**를 남긴다: 작업 전 `pnpm capture T-00N` → `screenshots/before/`로 옮기고, 작업 후 다시 캡처.
- 스크린샷은 **캡처한 코드와 같은 커밋 또는 바로 다음 커밋**에 넣는다. 화면과 코드가 어긋난 캡처는 기록이 아니다.

### PR 본문에 스크린샷 거는 법

저장소가 **private**이라 raw URL을 `![]()`로 임베드하면 로그인해 있어도 깨진다
(animal-league에서 겪었다). 대신:

- PR의 **Files changed 탭이 추가된 PNG를 그대로 보여준다** — 1차 리뷰 경로
- 본문에는 **커밋 SHA로 고정한 blob 링크**를 표로 단다 (브랜치명 링크는 force-push·삭제 후 깨진다)

```markdown
| 화면 | mobile | tablet | desktop |
|---|---|---|---|
| home | [보기](https://github.com/tlstkdgus/ULTSPOT/blob/<SHA>/docs/tasks/T-002/screenshots/home-mobile.png) | ... | ... |
```

- 본문에 이미지를 인라인으로 보이고 싶으면 PR 편집 화면에 PNG를 드래그&드롭한다 (선택)

---

## 5. 태스크 기록 — `docs/tasks/`

작업이 끝날 때마다 `docs/tasks/T-NNN/README.md`를 쓰고, `docs/tasks/README.md` 목록에 한 줄 추가한다.
양식은 [`docs/tasks/_template.md`](docs/tasks/_template.md).

세 기록이 답하는 질문이 다르다. 같은 내용을 복붙하지 않는다.

| 기록 | 답하는 질문 | 수명 |
|------|-------------|------|
| **커밋 본문** | 이 한 단위 변경은 왜 이렇게 했나 | 코드 줄과 함께 영구 (`git blame`) |
| **PR 본문** | 이 작업 묶음을 머지해도 되나 — 동작 표, 검증, 스크린샷 | 리뷰 시점 |
| **태스크 문서** | 이 작업은 무엇을 목표로 어디까지 했고, 다음은 뭔가 | 프로젝트 전체 이력 |

태스크 번호는 `docs/tasks/README.md` 목록의 마지막 번호 + 1.

---

## 6. PR

- **PR 하나 = 태스크 하나.** 서로 다른 화면을 한 PR에 섞지 않는다.
- 본문은 `.github/pull_request_template.md`를 채운다.
  - **동작 고정 표** — 라우팅·분기·권한처럼 경계가 있는 변경이면 `입력 → 기대 결과` 표. UI 변경이면 스크린샷 표가 그 역할을 한다.
  - **검증** — 실제로 돌린 것만, 숫자로. 안 해본 건 체크하지 않는다 (거짓 체크가 최악).
  - **머지 후 일어나는 일** — 자동 배포, 마이그레이션, 환경 변수 추가 등 사람이 해야 하는 것.
- **머지 전 셀프 리뷰**: `gh pr diff`를 처음부터 끝까지 한 번 읽는다. 디버그 로그, 주석 처리된 코드, 딸려 온 파일이 여기서 걸린다.

### 병합 — Merge commit (squash 금지)

```bash
gh pr merge --merge --delete-branch
```

animal-league는 Squash merge를 썼지만 ULTSPOT은 **squash하지 않는다.**
하루짜리 운영 도구였던 그쪽은 "PR 하나 = 되돌릴 커밋 하나"가 중요했고,
여기서는 작업 단위 커밋마다 판단 근거를 본문에 쓰기 때문에 squash하면 그 본문이 `main` 히스토리에서 사라진다.

처음에는 Rebase merge로 정했지만, #1·#2가 실제로 Merge commit으로 머지됐다. 커밋 본문을 보존한다는 목적은 둘 다 같아서
실제 습관에 맞춰 Merge commit을 기본으로 바꿨다. 되돌릴 때는 `git revert -m 1 <머지 커밋>`으로 PR 단위로 되돌릴 수 있다.
PR 안의 모든 커밋은 각각 빌드 가능해야 한다 (3장 규칙).

### 머지 후 — 프로덕션 확인

`main` 머지는 곧 Vercel 프로덕션 배포다 (제출 링크가 바로 바뀐다).
배포가 끝나면 `pnpm check:prod <프로덕션 URL>`을 돌리고, 결과를 태스크 문서 검증 칸에 적는다.
제출 요건과 마감 전 체크리스트는 [`docs/specs/submission-requirements.md`](docs/specs/submission-requirements.md).

---

## 7. 문서 전략 — README는 입구, 나머지는 docs/

| 문서 | 담는 것 | 담지 않는 것 |
|------|---------|--------------|
| `README.md` | 한 줄 소개, 스택, **clone 후 10분 안에 화면 띄우기**, 스크립트, 폴더 구조, 문서 지도 | 설계 결정의 긴 설명, 작업 이력 |
| `CONTRIBUTING.md` | 이 문서 — 브랜치·커밋·PR·캡처·기록 규칙 | 기능 설명 |
| `docs/design-system.md` | 토큰·컴포넌트 사용 규칙, 브랜드 가이드와의 대응 | 컴포넌트 props 전체 나열 (코드가 원천) |
| `docs/tasks/` | 태스크별 목표·진행·결정·스크린샷 | |
| `docs/specs/` | 기획안, PRD·기능명세서·유저플로우, 테마 테스트, 대회 제출 요건 | 구현 상세 (기능명세서는 동작 계약까지만 — 코드 구조는 코드가 원천) |

**README를 고쳐야 하는 PR**: 스크립트 추가/변경, 환경 변수 추가, 폴더 구조 변경, 필요한 설치 단계 변경.
PR 템플릿 체크리스트에 항목이 있다. README가 실제와 어긋나면 새로 온 사람이 첫 10분에 막힌다.

---

## 8. 커밋하지 말아야 할 것

- `.env*` (`.env.example` 제외) — Supabase 키, OpenAI 키
- `OPENAI_API_KEY`에 `NEXT_PUBLIC_` 접두사 — 브라우저 번들에 키가 박힌다
- 실제 사용자·팬 계정 정보가 담긴 시드 데이터, 저작권 있는 아티스트 사진 원본
- `test-results/`, `playwright-report/` — 실패 디버깅용 산출물 (gitignore 됨)
